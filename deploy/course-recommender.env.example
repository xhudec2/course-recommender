# Copy to /etc/course-recommender.env on the VM and chmod 600.
# Must match the CHAT_API_KEY the frontend's server-side proxy sends —
# /chat rejects requests with a missing/wrong X-API-Key header once this is set.
CHAT_API_KEY=...
